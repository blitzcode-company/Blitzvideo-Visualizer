import { DimensionesReproductor } from './dimensiones-reproductor';

describe('DimensionesReproductor', () => {
  it('should create an instance', () => {
    expect(new DimensionesReproductor()).toBeTruthy();
  });
});
