import { ConfiguracionReproductor } from './configuracion-reproductor';

describe('ConfiguracionReproductor', () => {
  it('should create an instance', () => {
    expect(new ConfiguracionReproductor()).toBeTruthy();
  });
});
