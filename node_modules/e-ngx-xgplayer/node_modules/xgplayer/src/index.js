import Player from './player'
import * as Controls from './control/*.js'

for (let k in Controls.control) {
    Player.install(Controls.control[k].name, Controls.control[k].method)
}
export default Player
